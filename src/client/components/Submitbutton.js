import React from "react";

const Submitbutton = props => (
    <div>
        <input

            className="submitbutton"
            type="submit"
        />
    </div>
);

export default Submitbutton;
